# Deploy Notes

## Netlify

- The GitHub repository for Minus 40 is already connected to Netlify.
- `git push origin main` is the normal path for updating the deployed Mini App.
- When the app needs to be checked on a phone, inspect the latest Netlify deploy first.
- If the app looks stale in Telegram, verify the Netlify deploy status before investigating the GitHub connection again.

## Working Assumption For Future Sessions

- Hosting is already set up.
- The remaining work is usually about app behavior, UI, Telegram runtime checks, or confirming that the latest deploy finished successfully.
