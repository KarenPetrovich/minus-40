# Routines

| Routine | Signature | Purpose |
| --- | --- | --- |
| replace_user_state | replace_user_state(uuid, jsonb) | Atomically replaces all weight entries for one user inside a single database transaction. |
